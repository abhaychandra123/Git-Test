code . == make VS folder

git init == initializes a repository

touch ==makes blank files

git add -A ==to add all untracked files to stage

git commit -m "Your message here "

git checkout index.html == Used to recover last commit 

git checkout -f == Recover all files from last commit

git log === see all commits

git diff == to see all the changes thAT were made

git rm waste.html == REMOVE also from staged and FOLDER

git rm --cached waste.html == Remove from stage but not from folder(untracked)

touch .gitignore == is a file that contains names of files that have to be ignorered